$('#board_form').replaceWith('<%= j render 'form', :board => @board, :remote => true %>');

if (($('#board_modal').data('bs.modal') || { isShown: false }).isShown == false) {
  $('#board_modal').modal('show');
}

window.init_tags();

window.init_sortable();
