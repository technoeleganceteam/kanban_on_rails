$('#new_issue').replaceWith('<%= j render 'form', :issue => @issue, :project => @project %>');

if (($('#new_issue_modal').data('bs.modal') || { isShown: false }).isShown == false) {
  $('#new_issue_modal').modal('show');
}

window.init_tags()
