= link_to_next_page items, (t '.next_page'),
  :class => 'next_page_infinite_scroll hidden', :remote => true,
  :params => { :section_id => section_id, :column_id => column_id,
    :controller => :issues, :action => :index, :board_id => board_id }
